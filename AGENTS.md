# AGENTS.md

## Pipeline Source Of Truth

- Use `PIPELINE.md` as the canonical pipeline for this repository.
- When asked about "the pipeline", "next item", or "what comes next", refer to `PIPELINE.md` first.
- Treat `app/static/app/js/classes/PipelineSteps.js` as the UI runtime subset only.
- Treat `DEVELOPMENT_STATUS.md` as implementation history and status notes, not the canonical ordered pipeline.